<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apptest2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".myPlaces" android:parentActivityName=".CustomerMenu"></activity>
        <activity android:name=".CustomerMenu" />
        <activity android:name=".EditProfileOwner" />
        <activity
            android:name=".ownerPrices"
            android:parentActivityName=".OwnerMenu" />
        <activity
            android:name=".OwnerAppoitnments"
            android:parentActivityName=".OwnerMenu" />
        <activity
            android:name=".ownerSaloon"
            android:parentActivityName=".OwnerMenu" />
        <activity android:name=".OwnerMenu" />
        <activity android:name=".CustomerOrSaloon" />
        <activity
            android:name=".LoginScreen"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>